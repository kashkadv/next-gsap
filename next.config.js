/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'avatars.githubusercontent.com', 'cloudflare-ipfs.com'],
  },
}

module.exports = nextConfig
